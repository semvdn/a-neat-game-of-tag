Keep both agents visible with dynamic pair camera framing

- treat manual camera zoom as the preferred maximum presentation zoom
- automatically zoom out when recursive branches separate the Runner and Chaser
- frame the full agent-body bounds with a fixed screen-space safety margin
- hard-clamp horizontal and vertical camera easing so smoothing cannot push either agent off-screen
- zoom out immediately for safety and ease back in gradually to avoid camera pumping
- keep presentation framing fully decoupled from physics and policy sensing
