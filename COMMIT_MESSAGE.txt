Prevent platform overlaps and recover escaped visual chasers

- enforce a hard non-overlap invariant across normal, recursive branch, merge, and moving platforms
- validate moving platforms against their complete oscillation sweep envelope, shrinking/switching motion when needed
- make Chaser escape require losing every Runner rather than being triggered by Runner-Runner separation
- teleport an escaped visual Chaser onto a safe platform behind the trailing Runner without resetting the world
- inherit the target Runner route and apply a brief tag re-arm delay after visual recovery
- restore and preserve the full Git history in packaged handoffs
