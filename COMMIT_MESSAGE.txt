Stabilize panorama and city landmark rendering

- sample continuous panorama silhouettes on a fixed world-space lattice
- preserve panorama vertical proportions during automatic camera zoom changes
- render landmark motifs in local pixel coordinates and translate them as rigid objects
- use fixed normalized city window layouts so facade details do not crawl or pop with zoom
- bump the visual biome cache version without changing terrain or simulation behavior
