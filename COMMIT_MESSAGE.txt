Remove background overlay seams

- replace segmented translucent panorama fills with one continuous gradient-filled silhouette per layer
- remove the full ridge highlight stroke that created U-shaped contour lines through valleys
- render snowy caps as contiguous ridge runs instead of one translucent quad per segment
- replace tiled sky color columns with continuous horizontal biome gradients
- replace tiled lower haze overlays with one continuous gradient fill
