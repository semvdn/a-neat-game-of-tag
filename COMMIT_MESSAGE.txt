Adopt continuous panorama backgrounds (Option B)

- replace far and mid chunk-stamped scenery with continuous biome-aware panoramas
- keep refined biome assets as sparse landmark overlays in mid/near/foreground layers
- make sky and haze spatially biome-aware across the full viewport
- reduce wide-camera popping with zoom-aware landmark fading and thinning instead of hard cutoffs
- lower landmark zoom clamp so assets continue shrinking naturally when the camera pulls far out
- enlarge descriptor cache adaptively to prevent live-window eviction during wide views
